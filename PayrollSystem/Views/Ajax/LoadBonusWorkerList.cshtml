@using Microsoft.AspNetCore.Identity
@model IEnumerable<WorkerVM>
@{
    Layout = null;
}
@foreach (var item in Model)
{
    <tr>
        <td>@item.Name</td>
        <td>@item.Surname</td>
        <td>@item.Position</td>
        <td>@item.Department</td>
        <td>@item.StartDate.ToShortDateString()</td>
        <td>
            <a asp-action="Create" asp-controller="Bonus" asp-route-id="@item.WorkerId">Add Bonus</a>
        </td>
    </tr>
}